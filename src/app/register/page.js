import { JwtRegisterView } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Register xOffer account',
};

export default function RegisterPage() {
  return <JwtRegisterView />;
}
